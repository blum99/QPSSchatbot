/**
 * Frontend Version Selector
 * 
 * Change FRONTEND_VERSION to switch between different UI implementations:
 * - 'v1': Original simple layout
 * - 'v2': Sidebar with drag-and-drop folders
 * - 'v3': Purple header, model selector (ILO/HEALTH, ILO/PENSION, etc.), resources/support tabs
 * 
 * After changing, restart the dev server: npm run dev
 */

module.exports = {
  FRONTEND_VERSION: 'v3', // Change this to 'v1', 'v2', 'v3', etc.
};
